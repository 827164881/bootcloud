server:
  port: 8005

spring:
  application:
    name: cloud-consumer-payment
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}